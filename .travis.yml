language: php

php:
  - 7.1

dist: trusty
sudo: false

addons:
  postgresql: 9.6

services:
  - rabbitmq

before_script:
  - travis_retry composer self-update
  - travis_retry composer install --no-interaction --prefer-source
  - php ./vendor/bin/parallel-lint -e php,phpt --exclude ./vendor .
  - psql -c 'create database herecsrymy_test;' -U postgres
  - cp ./tests/travis.neon ./app/config/local.neon

script: ./vendor/bin/tester -p php -c ./tests/php-unix.ini ./tests/HerecsrymyTests

after_failure:
  - 'for i in $(find ./tests -name \*.actual); do echo "--- $i"; cat $i; echo; echo; done'

cache:
  directories:
    - $HOME/.composer/cache
